Project Structure

src/index.js:
Main Cloudflare Worker entry point. Defines all API routes, integrates Workers AI for feedback analysis, and connects to the D1 database.

wrangler.jsonc:
Configuration file that defines Worker name and entry file, Workers AI binding and D1 database binding

